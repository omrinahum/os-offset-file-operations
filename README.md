# os-offset-file-operations
Offset-based file manipulation program in C. Demonstrates file I/O operations using fseek(), fread(), and fwrite() for reading and writing data at specific byte positions.
